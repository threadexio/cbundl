#include <stdio.h>          // Include standard input/output library

    // Main function, entry point of the program
    int main()             

    {                       // Start of the main function

        // Declare integer variable x and initialize it
        int x = 5;           // Variable 'x' is set to 5

        
        // Print the value of x to the console
        printf("Value of x: %d\n", x);  

        
        // Another dummy comment with no real purpose
        // Just filling space to make the code look bigger

            // Adding more unnecessary whitespace for no reason
            for (int i = 0; i < 3; i++)     // Loop to run 3 times
            {

                // Print a message in the loop
                printf("This is loop iteration: %d\n", i);

                
                // Another pointless comment
                // Just taking up space

            }   // End of for loop

        
        // Final comment that does nothing
        return 0;           // Return 0 to indicate successful completion

    }                       // End of main function
